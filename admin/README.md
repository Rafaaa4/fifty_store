# Fifty Store Admin

Standalone Vite dashboard for managing orders.

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

The admin app runs on `http://127.0.0.1:5174` and talks to the backend through `VITE_API_URL`.
